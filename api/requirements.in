fastapi>=0.140.13
uvicorn[standard]>=0.52.1
# 管理画面から chiezo-trigger(内部トリガーサービス)を叩くためのクライアント。
httpx>=0.28.1
# MCP サーバー(/mcp、Streamable HTTP)。REST と同じ関数を MCP ツールとしても公開する。
# 2.0 で `mcp.server.fastmcp.FastMCP` が `mcp.server.mcpserver.MCPServer` になった
# (移行済み)。3.x はまた作り直しになりうるので上限を張っておく。
mcp>=2.0,<3
