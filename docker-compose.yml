services:
  chiezo-api:
    build: ./api
    ports:
      # host:container。ホスト側だけ 9000 に変更(他アプリとの衝突回避)。コンテナ内部は 8000 のまま。
      - "9000:8000"
    volumes:
      - ./data:/data:ro
    environment:
      - CHIEZO_DATA_DIR=/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fs", "http://localhost:8000/healthz"]
      interval: 60s

  chiezo-ingest:
    build: ./ingest
    profiles: ["ingest"]
    volumes:
      - ./data:/data
    environment:
      - SOURCE=jawiki
      - CHIEZO_DATA_DIR=/data
